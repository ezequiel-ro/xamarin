<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Agenda.Views.MntDados" Title="Manutenção de dados">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Del" Command="{Binding DeleteCommand}"/>
        <ToolbarItem Text="Ok" Command="{Binding OkCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Primeira linha: nome-->
            <Label Grid.Row="0" Grid.Column="0" Text="Nome:" HorizontalOptions="End" VerticalOptions="Center"/>
            <Entry Grid.Row="0" Grid.Column="1" x:Name="txtNome" Placeholder="Nome" Keyboard="Text" Text="{Binding Nome}"/>

            <!-- Segunda linha: telefone-->
            <Label Grid.Row="1" Grid.Column="0" Text="Telefone:" HorizontalOptions="End" VerticalOptions="Center"/>
            <Entry Grid.Row="1" Grid.Column="1" x:Name="txtFone" Placeholder="Telefone" Keyboard="Text" Text="{Binding Telefone}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>